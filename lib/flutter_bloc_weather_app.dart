import 'package:flutter/material.dart';

class FlutterBlocWeatherApp extends StatelessWidget {
  const FlutterBlocWeatherApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Bloc Weather',
      theme: ThemeData.light(),
      home: const Scaffold(),
    );
  }
}
